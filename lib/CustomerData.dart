class CustomerData {
  String _Name, _Address, _MobileNo, _Amount;
  CustomerData({String name, address, mobileno, amount}) {
    _Name = name;
    _Address = address;
    _MobileNo = mobileno;
    _Amount = amount;
  }
}
